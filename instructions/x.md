kannst du eine einfache html page anlegen mit genau diesem logo und den hit points aus den werbemitteln und der gleichen farbgestaltung und design. Diese Seite soll auf dem hetzner server liegen  und einen Einstiegspunkt in das Testen der APIs darstellen. Ich möchte dem Kunden anstelle des curl - Befehls eine html/javascript internetseite anbieten, wo der die API austesten kann und zwar mit den Test eingaben, die wir auch verwendet haben. Es soll zwei Eingabemöglichkeiten geben einmal für validate und einmal für generate
Bei validate sollen zwei Textareas neben einander dargestellt sein und oben drüber die Zeile json in - validation out, die linke Textarea is mit "Input:" belabeled und das rechts mit Output: 
Beim Öffnen der Seite soll die Input Textarea mit dem fehlerbehafteten json Test Input belegt sein und das rechte soll leer sein.
unter den Textareas is ein Button, der sagt "Validate" und wenn der Benutzer diesen drückt, dann wird der json Input an den endpoint validate gesendet und was zurückkomt im rechten Textarea , die über eine senkrechte Scrollbar verfügt ausgegeben.
Der Benutzer soll hier seine eigene json Rechnung eingeben können und auf diese Weise validieren können .
Neben dem "Validate" Button sollen noch zwei Button angelegt sein, der eine hat den Text "Korrekte Rechnung" und der andere "Falsche Rechnung"
wenn der Benutzer korrekte Rechnung drückt, dann wird das Input json TextArea mit den fehlerfreien Testdaten und wenn der "fehlerhafte Rechnung" Button gedrückt wird, dann wird die Input TextArea mit den fehlerhaften Testdaten wie beim Seitenaufruf belegt.